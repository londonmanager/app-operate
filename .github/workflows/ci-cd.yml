name: Deploy to S3

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: arn:aws:iam::900201620692:user/mfe-arian-github-actions
          aws-secret-access-key: AKIA5DGBSUTKLIASPU6A
          aws-region: sa-east-1

      - name: Deploy to S3
        run: |
          aws s3 sync ./dist s3://londonmanager-frontend